engineblock_controllers:
    resource: '../src/OpenConext/EngineBlockBundle/Controller/'
    type: attribute
    host: "engine.{domain}"
    requirements:
        domain: ".*"
    defaults:
        domain: "%domain%"

engineblock_api_controllers:
    resource: '../src/OpenConext/EngineBlockBundle/Controller/Api'
    type: attribute
    host: "engine-api.{domain}"
    requirements:
        domain: ".*"
    defaults:
        domain: "%domain%"
