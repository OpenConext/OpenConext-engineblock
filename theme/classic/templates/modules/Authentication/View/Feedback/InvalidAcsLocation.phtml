<?php
// @todo this template does not seem to be used, nor does the route to it work

/**
 * @var Zend_Layout $layout
 */
$layout = $this->layout();
$layout->setLayout('1-column-blue-grey-narrow');

// Set Layout properties
$layout->title = $layout->title. ' - ' .$this->t('error_no_message');
$layout->header = $layout->title;
$layout->subheader = $this->t('error_no_message');
$layout->footerText = $this->t('footer');

?>
<div class="error-wrapper">
    <div class="error-icon"></div>
    <div class="error-text">
        <?= $this->t('error_invalid_acs_location');

        require_once realpath(__DIR__ . '/../../../Default/View/Error/include/footer.php');