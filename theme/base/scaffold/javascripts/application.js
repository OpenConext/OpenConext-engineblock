/**
 * Add your custom JS here.
 * By default, these are the contents we need to ensure everything works fine.
 * Feel free to remove these if you add custom JS of your own to replace their functionality.
 */

import {initializeIndex} from "../../base/javascripts";
import {initializeConsent} from "../../base/javascripts/consent";
import {initializeWayf} from "../../base/javascripts/wayf";

initializeIndex();
initializeWayf();
initializeConsent();
